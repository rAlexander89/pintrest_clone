
json.comment do 
  json.partial!
end
# json.comment do
  # json.partial! 'comment', comment: comment
    # json.extract! @comment, :id, :comment_txt, :user_id, :pin_id, :created_at, :updated_at
# end
  # @comments.each do |comment|
  #   json.set! comment.id do
  #     json.partial! 'comment', comment: comment
  #   end
  # end


  
  
  
  



